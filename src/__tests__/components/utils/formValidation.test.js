import { validateForm } from '../../../utils/formValidation';

describe('formValidation', () => {
  let values;

  it('should return empty array when no errors are found', () => {
    // values = {
    //   name: 'mockValidName',
    //   email: 'mockValidEmail@mockaroo.com',
    //   rating: 3,
    //   comment: 'mockValidComment'
    // };
    // const errors = validateForm(values);
    // expect(errors).toEqual({});
  });

});